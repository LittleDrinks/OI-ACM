---
tags:
  - 输出
  - 模板
---
```c++
inline int read()
{
    char c=getchar();
    int x=0, f=1;
    while (c < 48) {
        if (c == '-') { f=-1; }
        c = getchar();
    }
    while (c > 47) {
        x = (x<<1)+(x<<3)+(c^48);
        c = getchar();
    }
    return f*x;
}
```
