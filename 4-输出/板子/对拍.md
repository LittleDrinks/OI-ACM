---
tags:
---
```cpp
// checker.cpp
while (1) {
	system("A_gen > data.in");  // linux 下换成 ./xxx
	system("A_bf < data.in > A.ans");
	system("A < data.in > A.ans");
	if (system("fc A.out A.ans")) {  // linux 下换成 diff
		cout << "WA\n"; return 0;
	} else {
		cout << "AC\n";
	}
}
```
