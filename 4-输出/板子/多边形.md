---
tags:
  - 模板
  - 计算几何
---
```cpp
{   //点在凸包边上
    bool onSeg = false;
    for (int j = 0; j < 3; ++j) {
        if (onSegment(poly[j], poly[(j+1)%3], {a,b})) {
            onSeg = true;
        }
    }
    if (onSeg) { cout << "YES\n"; continue; }
}
{   // 点在凸包内部
    int cnt = 0;
    for (int j = 0; j < 3; ++j) {
        cnt += toLeft(poly[j], poly[(j+1)%3], {a,b});
    }
    if (abs(cnt) == 3) { cout << "YES\n"; }
    else               { cout << "NO\n"; }
}
```